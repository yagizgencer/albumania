# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Env
.env
# Any .env variant — backups, per-environment copies, scratch files. These hold
# live credentials and one nearly reached GitHub as a committed Resend API key.
.env.*
!.env.example

# Spotipy OAuth token cache (machine-local, contains access token)
.cache

# Databases
*.db
*.db-journal
*.sqlite
*.sqlite3

# Neon DB backups (contain user data — never commit)
backend/backups/

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Build artefacts
coverage/
.coverage
htmlcov/

# Claude Code personal permission overrides (machine-local)
.claude/settings.local.json

# Original static demo (album_ranking). It carries its own .git, so `git add`
# stages it as a gitlink with no submodule config — a clone would get an empty
# directory. Kept local; see CLAUDE.md ("port functions, don't import").
reference/
